services:
  - type: web
    name: crystalbeats-backend
    env: docker
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: PORT
        value: 8080
      - key: DATABASE_PASSWORD
        sync: false
      - key: JWT_SECRET
        sync: false
